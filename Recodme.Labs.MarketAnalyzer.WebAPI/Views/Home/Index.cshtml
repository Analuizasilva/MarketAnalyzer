@model Recodme.Labs.MarketAnalyzer.WebAPI.Models.Home.IndexViewModel

<h1>
    Top 2000 Companies in USA
</h1>
<table class="table table-hover" id="listTable">
    <thead>
        <tr>
            @foreach (var item in Model.HomeDataPocos)
            {
                <th class="w-1 no-sort"></th>
                <th class="w-1">@Html.DisplayNameFor(item => item)</th>
                <th class="w-1">@Html.DisplayNameFor(model => model.QuestionId)</th>
                <th class="no-sort"></th>
            }

        </tr>
    </thead>
</table>
<table class="table">
    <thead>
        <tr>
            <th></th>
        </tr>
    </thead>
    <tbody>
    </tbody>
</table>
